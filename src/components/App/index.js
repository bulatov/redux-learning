import React, { Component } from 'react';
import ShowPatientTable from '../../containers/ShowPatientTable.js';

export default class App extends Component {
  render() {
    return (
      <ShowPatientTable />
    );
  }
}
